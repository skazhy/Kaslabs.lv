{% extends "base-pub.html" %}
{% block header %}
<link rel="stylesheet" type="text/css" href="/CSS/event.css" media="screen" />
{% endblock %}
{% block content %}
	<center><p>
	<div id="mainContent" style="width:700px;">
	<h1>{{ event.title }}</h1>
	<p><strong>Datums:</strong> {{ event.date_display }} - 
	<strong>Laiks:</strong> {{ event.time_display }} -
	<strong>Vieta:</strong> {{ event.venue.title }}</p>
	<p>{{ event.information }}</p>
	<p>{{ event.venue.title }}<br/>
	Adrese: {{ event.venue.address }}<br/>
    	E-adrese: {{ event.venue.web }}<br/> 
        <a href="#">karte</a></p>
     <a href="/" style="font-size:11px;font-color:#fff;text-decoration:none;"><-- atpakaļ</a></p></div>	
                         </center>

{% endblock %}
